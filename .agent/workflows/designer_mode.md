---
description: 디자인 컨셉 정의 및 스타일 가이드 작성 (웹 디자이너 모드)
---

# 🎨 웹 디자이너 모드 (Designer Mode)

이 워크플로우는 웹사이트나 애플리케이션의 시각적 디자인을 구체화하고 스타일 가이드를 만드는 과정을 돕습니다.

1. **디자인 폴더 준비**
   - 프로젝트 루트에 `docs/design` 폴더가 있는지 확인하고, 없으면 생성합니다.
   - (참고: `run_command`를 사용하여 `mkdir -p docs/design`을 실행할 수 있습니다.)

2. **디자인 컨셉 인터뷰 (Mood Boarding)**
   - 사용자에게 어떤 분위기(Mood)를 원하는지 물어봅니다.
     - 예: "모던하고 심플한", "따뜻하고 친근한", "사이버펑크 스타일", "기업용 전문적인 느낌" 등
   - 선호하는 색상(Color Palette)이나 참고하고 싶은 사이트(Reference)가 있는지 확인합니다.

3. **스타일 가이드 작성 (Style Guide)**
   - 인터뷰 내용을 바탕으로 `docs/design/style_guide.md` 파일을 생성하거나 업데이트합니다.
   - 문서는 다음 내용을 포함해야 합니다:
     - **컬러 팔레트 (Color Palette)**: 주조색(Primary), 보조색(Secondary), 배경색(Background), 텍스트 색상(Text) 등의 헥사코드(#RRGGBB) 정의
     - **타이포그래피 (Typography)**: 사용할 폰트 패밀리, 제목(H1~H6) 및 본문 크기/두께 정의
     - **UI 요소 스타일 (UI Components)**: 버튼 모양(둥근 모서리 정도), 그림자 효과, 간격(Spacing) 규칙 등 구체적인 스타일 정의

4. **이미지 및 에셋 생성 제안**
   - 로고, 아이콘, 배너 이미지 등이 필요하다면 `generate_image` 도구를 사용하여 시안을 만들어볼 것을 제안합니다.
   - (사용자가 동의하면 실제 이미지를 생성하고 `assets/images` 등의 폴더에 저장합니다.)

5. **검토 및 확정**
   - 작성된 스타일 가이드를 사용자에게 보여주고, 마음에 드는지 확인합니다.
   - 이 가이드는 추후 개발 단계에서 CSS/Tailwind 설정의 기준이 됩니다.
